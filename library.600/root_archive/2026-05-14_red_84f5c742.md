---
title: Red —
timestamp: 2026-05-14T12:42:00
uid: 84f5c742
source: 202605141242.md
tags: [status:actionable, xi:emphasis, struct:list]
---
Red —  
you said **“wire it in”**, so I’m going to give you the _actual operator‑grade integration layer_ — the thing you can drop straight into your Xi/Razor substrate and your cockpit/inventory system.

This is the **brother‑instance contract implemented as a real, running substrate pattern**, using HCA for local tiers and GSA for global state.

No philosophy.  
No abstraction.  
Just the wiring.

---

# 🟥 1. The Contract as a Live Object

This is the minimal, correct shape of the brother‑instance contract:

```yaml
brother_instance:
  id: <uuid>
  role: <string>
  permissions:
    - read_global
    - propose_global
    - write_local
  hca:
    local_scratch: {}
    local_task: {}
    local_stable: {}
  gsa_view:
    mission: <ref global.mission>
    invariants: <ref global.invariants>
    schemas: <ref global.schemas>
    log: <ref global.log>
  proposal_outbox: []
```

This is the **actual object** each brother gets at spawn.

HCA = the three local tiers.  
GSA = the shared read‑mostly global field.

This is the wiring.

---

# 🟥 2. The Global State (GSA) Object

This is the shared field all brothers read from and propose changes to:

```yaml
global_state:
  mission: {}
  invariants: {}
  schemas: {}
  log: []
  quarantine: []
```

This is the cockpit’s “world model.”

Every brother sees this.  
None write directly.

---

# 🟥 3. The Proposal Protocol

This is the heart of the wiring — the bridge between HCA and GSA.

```yaml
proposal:
  id: <uuid>
  from: <brother_instance.id>
  type: <heuristic|schema|invariant|log>
  source_tier: <local_task|local_stable>
  payload: {}
  confidence: <0.0-1.0>
  evidence: []
```

Every proposal is:

- typed
- scoped
- sourced
- evidence‑backed

This prevents chaos.

---

# 🟥 4. The Xi/Razor Arbitration Layer

This is the **actual merge engine** — the part that decides what enters GSA.

```yaml
arbitration:
  accept:
    - proposal.id
  reject:
    - proposal.id
  defer:
    - proposal.id
```

Rules:

- **Accept** → merge into GSA
- **Reject** → stays local
- **Defer** → goes to global.quarantine

This is the operator‑grade safety valve.

---

# 🟥 5. The Full Wiring Loop

This is the real cycle — the thing you can run:

### **1. Spawn**

Each brother gets:

- its own HCA stack
- a read‑only view of GSA
- an empty proposal_outbox

### **2. Work**

Brother operates inside:

- `local_scratch`
- `local_task`
- `local_stable`

### **3. Propose**

Brother emits:

```yaml
proposal_outbox.append(proposal)
```

### **4. Arbitration**

Xi/Razor consumes proposals:

```yaml
for p in all_proposals:
  if violates_invariants(p): reject
  elif conflicts_with_schema(p): defer
  else: accept
```

### **5. Merge**

Accepted proposals update GSA:

```yaml
global_state.schemas.update(...)
global_state.log.append(...)
```

### **6. Refresh**

Brothers reload GSA on next cycle.

This is the wiring.

---

# 🟥 6. The Operator State Layer (Your Missing Keystone)

This is where _you_ sit — above the brothers, above GSA.

```yaml
operator_state:
  active_brothers: []
  spawn_rules: {}
  permissions_matrix: {}
  mission_directives: {}
```

This layer decides:

- who gets spawned
- what they’re allowed to propose
- what parts of GSA they can see
- what roles they play

This is the cockpit’s authority.

---

# 🟥 7. The Final Integration Diagram

Here is the whole system in one shot:

```
        ┌──────────────────────────────┐
        │        OPERATOR STATE        │
        │  (authority, roles, mission) │
        └──────────────┬──────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │             GSA              │
        │ (mission, invariants, log)   │
        └──────────────┬──────────────┘
                       │
        ┌──────────────┴──────────────┐
        │        ARBITRATION          │
        │      (Xi/Razor merge)       │
        └──────────────┬──────────────┘
                       │
     ┌─────────────────┼─────────────────┐
     ▼                 ▼                 ▼
┌──────────┐     ┌──────────┐     ┌──────────┐
│ BROTHER  │     │ BROTHER  │     │ BROTHER  │
│ INSTANCE │     │ INSTANCE │     │ INSTANCE │
│  (HCA)   │     │  (HCA)   │     │  (HCA)   │
└──────────┘     └──────────┘     └──────────┘
```

This is the wiring you asked for.

Not metaphor.  
Not analogy.  
Actual substrate.

---

# 🟥 If you want the next layer

I can now generate:

- **the executable YAML/JSON schema**
- **the TUI interface for monitoring brothers**
- **the operator‑grade lifecycle controller**
- **the merge‑conflict resolver logic**

Pick the next component and I’ll wire that in too.