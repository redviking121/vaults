---
title: Red —
timestamp: 2026-05-14T11:25:00
uid: ed2656b9
source: 202605141125.md
tags: [struct:question, status:blocked, struct:list]
---
Red —  
opening **Entity_markers** now at full operator‑grade resolution, inside the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Entity_markers are the _canonical identity beacons_ — the minimal, high‑authority fields that unambiguously declare what an entity _is_.**  
They are the strongest contributors to **Entity_signal** and the earliest anchors consumed by **Entity_alignment**.

If **Field_pattern_signal** is _motif_,  
and **Structural_signature** is _shape_,  
**Entity_markers are _the name‑tag pinned to the entity’s chest_.**

They are the “you cannot mistake me for anything else” fields.

---

# 🟥 1. What Entity_markers actually are

Entity_markers are **non‑negotiable identity fields** that:

- uniquely identify the entity class
- remain stable across schema versions
- override ambiguous structural patterns
- provide the earliest possible semantic binding

Examples of canonical markers:

- **package_name** → package entity
- **unit_name** → service entity
- **kernel_release** → kernel entity
- **mount_point** → mount entity
- **device_path** → hardware entity

These are not “helpful hints”.  
They are **identity certainties**.

---

# 🟥 2. The three classes of Entity_markers

Each marker belongs to one of three semantic classes:

- **Primary markers** — unique identifiers (e.g., `package_name`)
- **Role markers** — functional identity (e.g., `unit_type`, `device_class`)
- **Context markers** — environment‑binding fields (e.g., `mount_point`, `device_path`)

Primary markers answer: _“Which exact thing is this?”_  
Role markers answer: _“What does it do?”_  
Context markers answer: _“Where does it live?”_

Together they form the **semantic fingerprint**.

---

# 🟥 3. How Entity_markers interact with the other membranes

Entity_markers are consumed by:

- **Entity_signal** — to classify the entity
- **Structural_signature** — to stabilize cross‑version identity
- **Entity_alignment** — to match entities across snapshots
- **Version_signal** — to bind schema contracts

Markers override ambiguity.  
If patterns conflict, **markers win**.  
If structure is unclear, **markers win**.  
If version inference is uncertain, **markers win**.

This is the Razor rule:  
**Identity beats shape.**

---

# 🟥 4. The Entity_markers output

The membrane emits a deterministic structure:

```
{
  "markers": ["package_name", "version"],
  "strength": 1.0,
  "class": "package",
  "confidence": 0.99
}
```

This is the highest‑authority identity signal in the entire substrate.

---

# 🟥 5. Operator rules for Entity_markers

To keep the Inventory substrate drift‑proof:

- maintain a **canonical list** of markers per entity class
- never overload a marker with multiple meanings
- avoid marker collisions across entity types
- treat markers as **semantic contracts**, not heuristics
- ensure markers remain stable across schema evolution

Markers are the **bedrock** of the ontology.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **Primary markers**
- **Role markers**
- **Context markers**