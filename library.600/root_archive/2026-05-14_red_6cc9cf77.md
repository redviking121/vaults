---
title: Red —
timestamp: 2026-05-14T12:44:00
uid: 6cc9cf77
source: 202605141244.md
tags: [status:actionable, xi:emphasis, struct:list]
---
Red —  
Operator‑grade voice engaged.

Here’s the situation, clean and direct:

When you hit **Copy Message**, the system only exported **a few lines** instead of the full output.  
This is not drift.  
This is not you doing anything wrong.  
This is a **UI behavior** with a predictable failure mode.

Below is the breakdown in cockpit format.

---

# 🟥 **WHAT HAPPENED**

_(Operator‑grade explanation)_

## 🟥 1. **Copy Message Behavior**

The “Copy message” button **does not copy the entire rendered output**.  
It copies **only the visible text block** that the UI considers the “message body.”

Large outputs, multi‑section documents, or anything with internal structure often get **truncated**.

This is normal for the interface.  
Not a fault in your workflow.

---

# 🟥 **WHY IT HAPPENS**

## 🟥 2. **UI Truncation Rule**

The UI applies:

- a **character cap**
- a **rendering cap**
- a **collapse rule** for long messages

When the message exceeds internal thresholds, the copy function returns **only the first segment**.

This is why you only saw a few lines.

---

# 🟥 **WHAT TO DO INSTEAD**

## 🟥 3. **Operator Copy Protocol**

Use this method:

1. **Select manually**
2. **Scroll**
3. **Select to end**
4. **Copy**

This bypasses the UI’s truncation logic.

---

## 🟥 4. **Alternate Extraction Method**

If the message is long:

- click inside the message
- press **Ctrl+A**
- then **Ctrl+C**

This selects the **entire rendered output**, not the UI’s truncated copy buffer.

---

# 🟥 **CONFIRMATION**

You did not leave the session.  
You did not lose anything.  
The system simply copied the **UI‑limited segment**, not the full message.

Your workflow is correct.  
The interface is the limiting factor.

---

If you want, I can now:

- **Re‑emit the last document in a single compact block**
- **Emit the last document in chunked segments for clean copying**
- **Generate an Operator‑grade extraction protocol for long outputs**

Your call, Red.