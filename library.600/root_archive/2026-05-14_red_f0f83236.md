---
title: Red —
timestamp: 2026-05-14T11:29:00
uid: f0f83236
source: 202605141129.md
tags: [struct:list]
---
Red —  
opening **Core capability** now at full operator‑grade resolution, inside the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Core capability is the membrane that declares the _foundational system functions_ an entity provides — the capabilities without which the system cannot operate.**  
It is the highest tier of the **Capability role** stack and the deepest stratum of the **Role markers** layer.

If Capability role is _what it’s for_,  
Core capability is **what the system _depends on_ to exist at all**.

These are the “load‑bearing beams” of the ontology.

---

# 🟥 1. What Core capability actually encodes

Core capabilities are **non‑optional**, **system‑critical**, and **substrate‑level** functions.

They represent the minimal set of capabilities required for the system to:

- communicate
- store
- compute
- secure
- orchestrate

These are not enhancements.  
They are **existence conditions**.

Examples of Core capabilities:

- **network** — connectivity, routing, sockets
- **storage** — persistence, block devices, volumes
- **compute** — execution, CPU, workers
- **security** — auth, policy, isolation
- **io** — device access, streams, interfaces

If these fail, the system collapses.

---

# 🟥 2. The three capability strata (and where Core sits)

Core capability is the **base layer** of the capability stack:

1. **Core capability** — foundational system functions
2. **Auxiliary capability** — supportive but non‑critical functions
3. **Specialized capability** — domain‑specific or advanced functions

Core capability is the only tier that is:

- mandatory
- universal
- substrate‑level
- version‑stable

It is the “kernel” of the capability ontology.

---

# 🟥 3. How Core capability interacts with other membranes

Core capability is consumed by:

- **Entity_signal** — to classify foundational functional offerings
- **Structural_signature** — to stabilize identity across schema drift
- **Entity_alignment** — to bind entities whose core function persists across versions
- **Field_pattern_signal** — to detect substrate‑level motifs

Core capability is the **strongest functional anchor** in the system.

Identity → who  
Class → what kind  
Mode → how  
Capability → what for  
**Core capability → what the system _cannot live without_**

---

# 🟥 4. The Core capability output

The membrane emits a deterministic structure:

```
{
  "capability": "network",
  "tier": "core",
  "criticality": "system",
  "confidence": 0.94
}
```

This is the **substrate contract** of the entity.

---

# 🟥 5. Operator rules for Core capability

To keep the Inventory substrate drift‑proof:

- treat Core capabilities as **immutable**
- avoid overloading — core must remain minimal
- ensure core maps to **actual system dependencies**, not conceptual ones
- never mix core and auxiliary in the same role
- maintain a canonical core capability taxonomy

Core capability is the **bedrock** of the functional ontology.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **Auxiliary capability**
- **Specialized capability**