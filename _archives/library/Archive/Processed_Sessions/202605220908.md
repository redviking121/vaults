Absolutely — adding a **dry‑run mode** is the right call for a script you’ll only run occasionally and on potentially large batches. It gives you a safe “preview pass” where nothing is moved, nothing is written, and you can visually confirm exactly what will happen.

Below is a **clean, minimal patch** that adds:

- `--dry-run` flag
- clear preview output
- no file writes, no archives, no Zettel creation
- identical scanning and reporting logic

You’ll be able to run:

```
python3 intake-to-zettel.py --dry-run
```

…and see exactly what would happen.

---

# 🜂 **Patch: Add Dry‑Run Mode**

Replace your `main()` and `process_single_file()` with the versions below, and add the new `DRY_RUN` flag at the top.

## 1. Add this near the top of the file:

```python
import argparse

DRY_RUN = False
```

## 2. Replace `process_single_file()` with this version:

```python
def process_single_file(src: Path):
    if DRY_RUN:
        logging.info("[DRY‑RUN] Would process: %s", src)
        return

    logging.info("Processing %s", src)

    try:
        outpath = convert_session_to_zettel(src)
        logging.info("Wrote Zettel: %s", outpath)

        archive_file(src, ARCHIVE_PROCESSED)

    except Exception as e:
        logging.exception("FAILED to process %s: %s", src, e)
        archive_file(src, ARCHIVE_FAILED)
```

## 3. Replace `main()` with this version:

```python
def main():
    global DRY_RUN

    parser = argparse.ArgumentParser()
    parser.add_argument(
        "--dry-run",
        action="store_true",
        help="Preview actions without writing or moving files"
    )
    args = parser.parse_args()

    DRY_RUN = args.dry_run

    setup_logging()

    if DRY_RUN:
        logging.info("=== DRY‑RUN MODE ENABLED ===")
        logging.info("No files will be written, moved, or archived.")

    logging.info("Starting intake-to-zettel run")
    logging.info("INTAKE_DIR: %s", INTAKE_DIR)
    logging.info("ZETTEL_DIR: %s", ZETTEL_DIR)
    logging.info("ARCHIVE_ROOT: %s", ARCHIVE_ROOT)

    scan_and_process()

    logging.info("Run complete.")
```

---

# 🜁 **What Dry‑Run Mode Does**

When you run:

```
python3 intake-to-zettel.py --dry-run
```

You will see output like:

```
[DRY‑RUN] Would process: /home/red/Documents/red-vault/00_INTAKE/202605141524.md
[DRY‑RUN] Would process: /home/red/Documents/red-vault/00_INTAKE/202605141525.md
...
```

And **nothing** will be:

- written to `20_Zettels`
- moved to `processed/`
- moved to `failed/`
- logged as processed
- modified in any way

It is a perfect preview.

---

# 🜂 **Your directory prep is correct**

Everything you created:

```
~/Documents/Operator/intake-to-zettel/
    processed/
    failed/
```

…is exactly what the script expects.

You’re ready to run a dry‑run safely.

---

If you want, I can also add:

- **a `--limit N` flag**
- **a `--verbose` mode**
- **a summary report at the end**

Just tell me what you want next.