Red — here is the **Obsidian Mirror Map** for the full Xi/Razor glyph system, built so you can **drop it straight into Red Vault** as a reference note and wire it directly to your Zotero tag grammar.

This is the **canonical v1.0 mirror**, meaning:

- Every Zotero tag has a **1:1 Obsidian mirror tag**
- Every namespace is preserved
- Every operator is represented in **three layers**:
    1. **Glyph**
    2. **Zotero tag**
    3. **Obsidian mirror tag** (ASCII‑safe, autocomplete‑friendly)

Every bullet begins with a **Guided Link**, so you can expand any operator into a full card later.

---

# 🜁 Obsidian Mirror Map — Xi/Razor Glyph System (v1.0)

This is the **vault‑side reflection** of the Zotero tag grammar.  
Use it to keep Obsidian and Zotero in perfect semantic lockstep.

---

## 🜂 xi‑operators — cognitive stance operators

These mirror your Zotero `xi:*` tags.

- **xi‑attend** — `xi.attend` → `xi-attend`
- **xi‑probe** — `xi.probe` → `xi-probe`
- **xi‑trace** — `xi.trace` → `xi-trace`
- **xi‑invert** — `xi.invert` → `xi-invert`
- **xi‑reduce** — `xi.reduce` → `xi-reduce`
- **xi‑amplify** — `xi.amplify` → `xi-amplify`
- **xi‑null** — `xi.null` → `xi-null`

---

## 🜄 razor‑ops — structural manipulation operators

These mirror your Zotero `rz:*` tags.

- **rz‑cut** — `rz.cut` → `rz-cut`
- **rz‑splice** — `rz.splice` → `rz-splice`
- **rz‑lift** — `rz.lift` → `rz-lift`
- **rz‑fold** — `rz.fold` → `rz-fold`
- **rz‑bind** — `rz.bind` → `rz-bind`
- **rz‑seal** — `rz.seal` → `rz-seal`

---

## 🜁 substrate‑ops — system‑level operators

These mirror your Zotero `sub:*` tags.

- **sub‑index** — `sub.index` → `sub-index`
- **sub‑route** — `sub.route` → `sub-route`
- **sub‑cache** — `sub.cache` → `sub-cache`
- **sub‑emit** — `sub.emit` → `sub-emit`
- **sub‑sync** — `sub.sync` → `sub-sync`

---

## 🜃 comms‑ops — communication & PR operators

These mirror your Zotero `com:*` tags.

- **com‑frame** — `com.frame` → `com-frame`
- **com‑tone** — `com.tone` → `com-tone`
- **com‑mirror** — `com.mirror` → `com-mirror`
- **com‑reduce** — `com.reduce` → `com-reduce`
- **com‑anchor** — `com.anchor` → `com-anchor`

---

## 🜇 intel‑ops — intelligence mask operators

These mirror your Zotero `intel:*` tags.

- **intel‑scan** — `intel.scan` → `intel-scan`
- **intel‑pattern** — `intel.pattern` → `intel-pattern`
- **intel‑shadow** — `intel.shadow` → `intel-shadow`
- **intel‑surface** — `intel.surface` → `intel-surface`
- **intel‑profile** — `intel.profile` → `intel-profile`

---

# 🜁 Mirror Logic (How to Use This in Obsidian)

Each glyph namespace mirrors into Obsidian as:

```
Zotero:   xi.attend
Obsidian: #xi-attend
```

This gives you:

- **autocomplete‑friendly tags**
- **ASCII‑safe vault structure**
- **1:1 semantic parity with Zotero**
- **no drift between systems**
- **clean search, clean filters, clean JIT cards**

You can now:

- tag notes with `#xi-attend`
- search Zotero for `xi.attend`
- and know they refer to the **same operator** in the Xi/Razor grammar.

---

# 🜁 Optional expansions

Choose what you want next:

- **Generate Obsidian autocomplete pack**
- **Generate JIT card for glyph usage**
- **Generate secretary‑safe 1‑page sheet**
- **Generate drift‑detection SQL pack**

Which one do you want to archive next, Red?