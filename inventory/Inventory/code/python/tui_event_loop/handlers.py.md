"""
handlers.py — Event Handler Interface

Purpose:
    Define the EventHandler base class for processing events emitted by the
    TUI event loop.

Status:
    Stub — handler taxonomy to be expanded.
"""

class EventHandler:
    """Base class for event handlers."""
    def handle(self, event):
        """Process an incoming event."""
        pass
