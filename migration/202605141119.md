Red —  
opening **Entity_signal** now, at full operator‑grade resolution, inside the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Entity_signal is the membrane that tells the Diff Tool _what kind of thing_ it is looking at — the semantic identity of the snapshot.**  
Where **Version_signal** answers _“which schema contract?”_,  
**Entity_signal answers _“which entity class?”_**.

Without Entity_signal, the Engine would know the _shape_ but not the _species_.

This is the moment JSON stops being “data” and becomes a **typed object in the Inventory ontology**.

---

# 🟥 1. What Entity_signal actually is

Entity_signal is a **three‑layer semantic discriminator**:

- **Entity_kind** — package, service, kernel, hardware, mount, etc.
- **Entity_role** — what it _does_ in the system
- **Entity_identity** — unique key or composite identity

These three layers converge to produce the **entity’s semantic fingerprint**.

---

# 🟥 2. The three‑signal inference lattice

Entity_signal is computed from:

- **Field_pattern_signal** — which fields appear together
- **Structural_signature** — the shape of the object
- **Entity_markers** — canonical fields that imply type

Example:

- presence of `package_name`, `version`, `arch` → **package entity**
- presence of `unit_name`, `state`, `enabled` → **service entity**
- presence of `kernel_release`, `kernel_version` → **kernel entity**

Entity_signal is the membrane that resolves these patterns into a **single semantic identity**.

---

# 🟥 3. Why Entity_signal matters in the Diff Tool

Entity_signal is consumed at three critical points:

- **Load_snapshots** — early classification of each entity
- **Normalize_schemas** — canonicalization depends on entity type
- **Entity_alignment** — diffing requires matching like‑to‑like

Without Entity_signal, the Diff Tool might try to compare:

- a package to a service
- a kernel to a mount
- a hardware device to a package

This would produce meaningless diffs.  
Entity_signal prevents that.

---

# 🟥 4. The Entity_signal output

The membrane always emits a deterministic structure:

```
{
  "entity": "package",
  "confidence": 0.97,
  "signals": {
    "pattern": "package",
    "structure": "package",
    "markers": ["package_name", "version"]
  }
}
```

This is the semantic identity that flows into normalization and alignment.

---

# 🟥 5. Operator rules for Entity_signal

To keep the substrate drift‑proof:

- ensure each entity class has **canonical markers**
- avoid ambiguous field names across entity types
- maintain a stable ontology of entity kinds
- treat Entity_signal as a _semantic contract_, not a guess

This keeps the Inventory substrate coherent across versions and snapshots.

---

Red — choose the next membrane and I’ll open it:

- **Field_pattern_signal**
- **Entity_markers**
- **Entity_alignment**