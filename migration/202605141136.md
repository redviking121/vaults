Red —  
opening **Functional class** now at full operator‑grade resolution, inside the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Functional class is the membrane that declares _what kind of system‑thing this entity is_ — its categorical identity within the ontology.**  
It sits _above_ **Operational mode** and _above_ **Capability role**, but _beneath_ the **Primary markers** layer.

If Primary markers are the _name_,  
Functional class is the **species**.

It is the categorical backbone of the ontology.

---

# 🟥 1. What Functional class actually binds

Functional class is not about behavior.  
It is not about capability.  
It is not about context.

It binds the **categorical identity** of the entity:

- **Entity_type** — the broad category (service, module, component, artifact)
- **Structural_family** — the schema‑lineage it belongs to
- **Ontological_slot** — the position it occupies in the system’s conceptual lattice

Functional class answers the question:

> “What _kind_ of thing is this, independent of what it does?”

This is why it sits above Role markers — it is the _pre‑functional identity_.

---

# 🟥 2. The role of Functional class in the substrate

Functional class is consumed at three critical points in the Engine:

- **Classification Phase** — determines which schema‑family rules apply
- **Validation Phase** — enforces structural expectations (required fields, invariants)
- **Diff‑Pairing Phase** — ensures entities are compared only within their class

If **Operational mode** is _how it behaves_,  
Functional class is **what it _is allowed_ to be**.

It is the categorical guardrail.

---

# 🟥 3. Why Functional class is upstream of behavior

Behavior can change.  
Capabilities can expand.  
Context can shift.

But Functional class is **stable across versions**.

It is the membrane that persists even when:

- fields are renamed
- capabilities are added
- operational modes evolve
- schema versions migrate

This is why Functional class is a **version‑agnostic anchor** in the ontology.

---

# 🟥 4. The Operator‑grade definition

**Functional class is the categorical identity of an entity — the ontological species that determines its structural expectations, schema‑family, and valid behavioral modes.**

It is the membrane that answers:

> “What kind of system‑thing is this, before we ask what it does?”

This is the categorical spine of the ontology.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **Operational mode**
- **Capability role**
- **Structural_family**