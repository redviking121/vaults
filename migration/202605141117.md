Red —  
re‑opening **schema_version**, but this time I’ll take it **one layer deeper** and bind it directly into the **Inventory_diff_tool_extension** membrane you just invoked.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**schema_version is the contract‑anchor that makes an Inventory entity _time‑aware_.**  
It is the field that tells the Engine: _“interpret this snapshot using schema vX.Y.Z — no guessing.”_

When present, it dominates the entire **Version_signal** lattice.

---

# 🟥 1. The role of schema_version inside the Diff Tool

Within the Diff Execution Path, schema_version is consumed at three critical points:

- **Load_snapshots** — early binding of entity → version
- **Schema_detection** — version lock overrides inference
- **Normalize_schemas** — canonicalization uses declared version

This ensures the Diff Tool never compares mismatched shapes (e.g., v0.2 vs v0.4) without first reconciling them.

---

# 🟥 2. The semantic versioning rule

schema_version always follows:

```
MAJOR.MINOR.PATCH
```

- **MAJOR** — breaking shape changes
- **MINOR** — additive fields
- **PATCH** — clarifications, non‑shape fixes

This is the same rule enforced by **Explicit_version_field**, but schema_version is the _canonical_ form.

---

# 🟥 3. How schema_version interacts with other version signals

The version inference stack is strictly hierarchical:

1. **schema_version** — explicit, operator‑declared
2. **Structural_signature** — inferred from geometry
3. **Field_presence_vector** — fallback heuristic

If schema_version exists, the other two become **validators**, not deciders.

If schema_version is absent, the other two become **deciders**, not validators.

---

# 🟥 4. Why schema_version is mandatory for stable diffing

Without schema_version:

- the Engine must infer shape
- inference can drift over time
- diffs become probabilistic
- normalization becomes lossy
- deltas become ambiguous

With schema_version:

- shape is deterministic
- normalization is exact
- diffs are stable
- deltas are reproducible

This is why the Xi/Razor substrate treats schema_version as a **stability anchor**.

---

# 🟥 5. Operator rules for schema_version

To maintain substrate integrity:

- declare schema_version once schema stabilizes
- increment **MINOR** when adding fields
- increment **MAJOR** when removing/renaming fields
- increment **PATCH** for non‑shape clarifications
- never rewrite history — create new versions instead

This keeps the Library and Inventory substrates drift‑proof.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **inventory_version**
- **format_field**
- **Version_signal**