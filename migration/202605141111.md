Red —  
opening **Load_snapshots** now, at full operator‑grade resolution, in the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Load_snapshots is the membrane where raw host‑state becomes _temporal material_.**  
This is the moment two independent Inventory captures are pulled into the Engine, normalized, and prepared for schema‑level comparison.

Before this step, you have _files_.  
After this step, you have _state_.

---

# 🟥 1. Load_snapshots is a two‑channel ingestion membrane

The Diff Tool always ingests snapshots through two deterministic channels:

- **Primary_snapshot** — the “current” or “newer” host‑state
- **Reference_snapshot** — the “baseline” or “older” host‑state

Both channels must resolve to **InventorySnapshot** objects before the Diff Tool can proceed.

This membrane enforces _temporal directionality_.

---

# 🟥 2. The ingestion boundary enforces three invariants

Load_snapshots always validates:

- **Existence_invariant** — both snapshot paths must resolve to readable artifacts
- **Schema_invariant** — both snapshots must match the Inventory schema version
- **Identity_invariant** — both snapshots must describe the same host identity

If any invariant fails, the membrane collapses and the Diff Tool aborts.

---

# 🟥 3. The membrane produces two canonical objects

After ingestion and validation, the membrane yields:

- **Canonical_primary** — normalized, schema‑aligned, host‑verified
- **Canonical_reference** — same structure, same guarantees

These are the objects that flow into **Normalize_schemas**.

---

# 🟥 4. Why this membrane matters

Load_snapshots is the **first temporal gate**.  
It ensures the Diff Tool never compares:

- mismatched schema versions
- snapshots from different hosts
- corrupted or partial captures
- unordered or ambiguous temporal states

This is the membrane that guarantees **temporal integrity**.

---

# 🟥 5. The execution geometry

Inside the Diff Tool’s `run()` method, the membrane looks like:

```
let primary = self.load_snapshot(&args.primary_path)?;
let reference = self.load_snapshot(&args.reference_path)?;
```

But the behavioral geometry is:

1. Resolve → 2. Validate → 3. Normalize → 4. Canonicalize

This is the **temporal intake pipeline**.

---

Red — choose the next membrane and I’ll open it:

- **Normalize_schemas**
- **Compute_diffs**
- **Emit_delta**