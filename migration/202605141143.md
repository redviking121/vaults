Red —  
opening **Detection_rules** at full operator‑grade resolution, down at the level where the membrane becomes _pure discriminative machinery_ rather than “error handling”.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Detection rules are the membrane that determines _when an entity is in an invalid state_.**  
They are the _first line of defense_ in **Error semantics** — the discriminators that prevent silent corruption.

If **Readiness semantics** prevents invalid starts,  
Detection rules prevent **invalid continuations**.

---

# 🟥 1. What Detection rules _are_

Detection rules are the **runtime invariants and discriminators** that determine:

- whether the entity is still valid
- whether its inputs are valid
- whether its internal state is coherent
- whether the substrate contract is being violated
- whether execution may safely continue

They are the **error‑recognition membrane**.

Where **Execution semantics** defines _how_ the entity runs,  
Detection rules define _whether it may continue running_.

---

# 🟥 2. The four discriminative layers of Detection

Each entity expresses detection through four stacked layers:

- **Precondition_checks** — validity of inputs and entry conditions
- **Invariant_checks** — structural truths that must remain stable
- **State_integrity_checks** — coherence of internal state
- **Substrate_contract_checks** — compliance with system‑level rules

These four layers form the **detection spine**.

---

# 🟥 3. Precondition checks

This layer ensures:

- required fields exist
- types and shapes match expectations
- the entity is invoked in a legal context
- no forbidden combinations are present
- no missing dependencies

This is the membrane that prevents **invalid entry into a function or phase**.

It is the runtime expression of **DSR‑1: Declare → Stabilize → Propagate** at the moment of invocation.

---

# 🟥 4. Invariant checks

This layer enforces truths that must _always_ hold:

- structural invariants
- ordering invariants
- monotonicity invariants
- identity invariants
- relational invariants

If an invariant breaks, the entity is **no longer itself**.

This is the membrane that protects the **entity’s identity geometry**.

---

# 🟥 5. State integrity checks

This layer ensures:

- internal fields are coherent
- no contradictory states exist
- no partial updates remain
- no illegal transitions occurred
- no drift has accumulated

This is the membrane that prevents **semantic rot**.

It is the runtime counterpart to **Lifecycle semantics**.

---

# 🟥 6. Substrate contract checks

This layer ensures the entity is not violating:

- system‑level invariants
- resource constraints
- scheduling rules
- capability boundaries
- membrane boundaries

This is the membrane that prevents **system‑wide corruption**.

It is the enforcement arm of the **Xi/Razor substrate**.

---

# 🟥 7. Why Detection rules matter

Detection rules ensure:

- early failure
- non‑corrupting failure
- predictable error classification
- safe propagation
- stable recovery
- substrate integrity

Without Detection rules, errors become **latent**, and latent errors become **catastrophic**.

With them, the entity becomes **safe, predictable, and composable**.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **Precondition_checks**
- **Invariant_checks**
- **State_integrity_checks**
- **Substrate_contract_checks**