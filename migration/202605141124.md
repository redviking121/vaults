Red —  
opening **Entity_alignment** now at full operator‑grade resolution, inside the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Entity_alignment is the membrane that ensures the snapshot’s _identity layer_ is structurally coherent across all other version‑bearing signals.**  
If **Entity_signal** tells the Engine _what kind of thing this is_,  
**Entity_alignment tells the Engine _how tightly that identity binds across the other membranes_.**

It is the _coherence check_ that prevents the system from mistaking “similar shape” for “same entity”.

---

# 🟥 1. What Entity_alignment _actually binds_

Entity_alignment is not semantic.  
It is not interpretive.  
It is not inferential.

It binds three structural surfaces:

- **Entity_signal** — identity
- **Version_signal** — contract
- **Structural_signature** — behavioral fingerprint

Entity_alignment ensures these three membranes **agree on the same entity‑class**.

If any one of them diverges, the Engine raises a structural mismatch before diffing begins.

---

# 🟥 2. Why Entity_alignment exists

Because in a real Inventory substrate:

- fields drift
- schemas evolve
- versions fork
- entities mutate
- snapshots degrade
- inference becomes dangerous

Entity_alignment is the membrane that prevents the Engine from “guessing” identity based on superficial similarity.

It enforces:

- **identity coherence**
- **contract coherence**
- **signature coherence**

This is how the system avoids false positives in the diff path.

---

# 🟥 3. The three alignment checks

Entity_alignment performs three deterministic checks:

### **1. Identity‑to‑Contract Alignment**

Does the declared entity type match the declared schema contract?

(Example: “Package” must align with schema “package.v3”.)

### **2. Identity‑to‑Signature Alignment**

Does the entity’s behavioral fingerprint match the expected signature for that entity class?

(Example: a “User” entity must exhibit the User signature motifs.)

### **3. Contract‑to‑Signature Alignment**

Does the schema contract’s expected field behavior match the observed structural signature?

(Example: schema v2 expects field X to be stable; signature must reflect that.)

These three checks form a **triangular lock**.

---

# 🟥 4. What Entity_alignment is _not_

- not a diff
- not a merge
- not a reconciliation
- not a semantic interpretation
- not a schema inference
- not a fallback mechanism

It is a **pre‑diff structural gate**.

If Entity_alignment fails, the Engine halts before any comparison occurs.

---

# 🟥 5. Relationship to other membranes

Entity_alignment sits directly above:

- **Field_presence_vector** (raw existence)
- **Field_pattern_signal** (motif)
- **Structural_signature** (behavioral fingerprint)

And directly below:

- **Version_signal** (contract binding)
- **schema_version** (explicit contract)
- **inventory_version** (epoch binding)

It is the **identity‑coherence membrane** that stabilizes the entire diff pipeline.

---

# 🟥 6. Failure modes

Entity_alignment fails when:

- Entity_signal contradicts schema_version
- Structural_signature contradicts Entity_signal
- Field_pattern_signal contradicts expected entity motifs
- Version_signal contradicts declared entity class
- The snapshot exhibits hybrid or ambiguous identity patterns

When this happens, the Engine does not attempt inference.  
It raises a **structural identity mismatch**.

This is by design.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **Entity_markers**
- **Structural_signature**
- **Version_signal**