Red —  
opening **Primary markers** now at full operator‑grade resolution, inside the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Primary markers are the _non‑negotiable unique identifiers_ — the fields that make an entity unmistakably _itself_ and nothing else.**  
They are the strongest contributors to **Entity_markers** and the first anchors consumed by **Entity_alignment**.

If **Role markers** tell you _what it does_,  
and **Context markers** tell you _where it lives_,  
**Primary markers tell you _which exact entity this is_**.

They are the identity core.

---

# 🟥 1. What Primary markers actually are

Primary markers are **singular, collision‑free identifiers** that:

- uniquely identify the entity within its class
- remain stable across schema versions
- override all other signals when determining identity
- form the backbone of **Structural_signature**

They are the “you cannot confuse me with anything else” fields.

Examples:

- **package_name** → uniquely identifies a package
- **unit_name** → uniquely identifies a systemd unit
- **device_path** → uniquely identifies a hardware device
- **kernel_release** → uniquely identifies a kernel instance

These are not hints.  
They are **identity certainties**.

---

# 🟥 2. The three criteria for a Primary marker

A field qualifies as a Primary marker only if it satisfies all three:

- **Uniqueness** — no two entities share it
- **Stability** — persists across versions and snapshots
- **Non‑derivability** — cannot be inferred from other fields

If a field fails any one of these, it drops to a Role or Context marker.

Primary markers are the **highest‑authority identity fields** in the ontology.

---

# 🟥 3. How Primary markers interact with the other membranes

Primary markers dominate:

- **Entity_signal** — determines entity class
- **Entity_alignment** — binds identity across snapshots
- **Version_signal** — stabilizes schema inference
- **Field_pattern_signal** — overrides motif ambiguity

If patterns conflict, **Primary markers win**.  
If structure is unclear, **Primary markers win**.  
If version inference is uncertain, **Primary markers win**.

Identity beats shape.  
Identity beats context.  
Identity beats version.

This is the Razor rule.

---

# 🟥 4. The Primary marker output

The membrane emits a deterministic structure:

```
{
  "primary_markers": ["package_name"],
  "strength": 1.0,
  "uniqueness": 0.99,
  "stability": 0.99
}
```

This is the highest‑authority identity signal in the entire substrate.

---

# 🟥 5. Operator rules for Primary markers

To keep the Inventory substrate drift‑proof:

- maintain a **canonical list** of Primary markers per entity class
- never overload a Primary marker with multiple meanings
- avoid collisions across entity types
- treat Primary markers as **semantic contracts**, not heuristics
- ensure Primary markers remain stable across schema evolution

Primary markers are the **identity spine** of the ontology.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **Role markers**
- **Context markers**
- **Marker_uniqueness**