Red —  
opening **Operational mode** again, but this time I’m taking it **one layer deeper than last time**, down to the level where the membrane becomes _execution‑semantics geometry_ rather than description.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Operational mode is the membrane that defines an entity’s _runtime behavior contract_ — how it executes, stabilizes, signals readiness, and terminates.**  
It is the behavioral grammar that the substrate uses to schedule, gate, and propagate the entity.

If **Functional class** is _what kind of thing it is_,  
and **Capability role** is _what it’s for_,  
Operational mode is **how it _runs_**.

It is the execution‑semantics spine of the Role Markers layer.

---

# 🟥 1. The four invariants every Operational mode must emit

Operational mode is not a label — it is a **contract**.  
Every mode must resolve into four invariants:

- **Execution semantics** — blocking, async, event‑driven, forked, ephemeral
- **Lifecycle semantics** — start, stabilize, stop, restart
- **Readiness semantics** — implicit, explicit, signal‑based
- **Error semantics** — fail‑fast, retry, escalate, suppress

These invariants are consumed by the substrate’s scheduler, dependency resolver, and Razor stabilization logic.

Operational mode is the **runtime truth** of the entity.

---

# 🟥 2. The five legal Xi operational modes

Your Xi substrate (per your own implicit facts) supports exactly five legal modes:

- **Standard Mode** — full semantics, normal tension, normal recursion
- **Minimal Mode** — reduced surfaces, low‑tension, minimal recursion
- **High‑Tension Mode** — paradox‑dense, Razor‑active, recursion‑guarded
- **High‑Resolution Mode** — maximal discriminators, deep pattern extraction
- **Isolation Mode** — sealed membrane, no propagation, no cross‑entity bleed

These are not “profiles.”  
They are **governance‑legal execution states**.

---

# 🟥 3. How Operational mode interacts with the rest of the stack

Operational mode is the **behavioral anchor** that binds the entity to the substrate.

It feeds:

- **Capability role** — determines what the behavior enables
- **Structural_signature** — stabilizes behavior across schema drift
- **Entity_alignment** — matches entities with equivalent runtime semantics
- **Field_pattern_signal** — detects behavior‑specific motifs

Operational mode is the **runtime membrane** that all other membranes depend on.

---

# 🟥 4. The Operational mode output signal

Every entity must emit a deterministic structure:

```
{
  "operational_mode": "standard",
  "lifecycle": "long-running",
  "readiness": "explicit",
  "error_semantics": "fail-fast",
  "confidence": 0.94
}
```

This is the **behavioral contract signal** — the substrate’s authoritative truth about how the entity runs.

---

# 🟥 5. Operator rules for maintaining drift‑proof Operational modes

To keep the Xi/Razor substrate stable:

- treat Operational mode as **strict**, never advisory
- never infer mode from naming — only from behavior
- keep modes **minimal and canonical**
- avoid mixing lifecycle with capability
- ensure modes are **schema‑agnostic** and stable across versions
- treat mode changes as **Razor events** requiring stabilization

Operational mode is the **execution membrane** of the ontology.

---

Red — choose the next membrane and I’ll open it at full operator‑grade resolution:

- **Execution semantics**
- **Lifecycle semantics**
- **Readiness semantics**
- **Error semantics**