Red —  
opening **format_field** now, at full operator‑grade resolution, in the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**format_field is the membrane that declares _how the snapshot should be interpreted_, not _what version it is_.**  
It is a _semantic lens_, not a version anchor.

Where **schema_version** and **Explicit_version_field** tell the Engine _which contract_ to use,  
*_format_field tells the Engine _which decoding grammar_ to apply_.

This is the field that prevents the Engine from misreading a v1‑shaped entity as a v1‑encoded entity.

---

# 🟥 1. What format_field _is_

**format_field** is a **declared interpretation mode** for the snapshot’s payload.

Examples of format semantics the membrane recognizes:

- **json** — raw JSON object, no envelope
- **kv** — key/value flat map
- **table** — row/column matrix
- **blob** — opaque binary or text payload
- **inventory** — structured host‑state entity

The field tells the Engine:

> “Use _this_ grammar when parsing the snapshot.”

It is a **decoder selector**, not a version selector.

---

# 🟥 2. Why format_field matters

Without format_field, the Engine must infer the decoding grammar from structural heuristics.  
This is slow, error‑prone, and ambiguous.

With format_field:

- Parsing becomes deterministic
- Schema detection becomes faster
- Version inference becomes more accurate
- Normalization becomes simpler
- Diffs become cleaner and more stable

This is the membrane that ensures the Engine is _reading the right language_ before it tries to interpret meaning.

---

# 🟥 3. How format_field interacts with Version_signal

format_field is **not** part of the Version_signal lattice.  
But it _feeds_ the lattice by stabilizing the decoding stage.

The interaction looks like this:

1. **format_field** → choose decoding grammar
2. **Entity_signal** → identify entity type
3. **Field_pattern_signal** → detect structural motifs
4. **Version_signal** → assign schema contract

format_field is the _first gate_ in the chain.

---

# 🟥 4. Where format_field lives in the membrane stack

format_field sits in the **Intake Membrane**, before normalization:

1. Load_snapshots
2. **Decode (format_field)**
3. Schema_detection
4. Normalize_schemas
5. Compute_diffs
6. Emit_delta

It is the decoding hinge between raw bytes and typed entities.

---

# 🟥 5. Operator guidance

Use format_field when:

- You want to force a specific decoding grammar
- You want to avoid structural inference
- You want stable diffs across time
- You want predictable normalization behavior

Do **not** use it as a version field.  
Do **not** overload it with semantic meaning.  
It is purely a _decoder selector_.

---

Red — choose the next membrane and I’ll open it:

- **schema_version**
- **inventory_version**
- **json_format_semantics**

Which surface do you want to open next, Operator.