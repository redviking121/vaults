Red —  
opening **Field_presence_vector** now, at full operator‑grade resolution, inside the same Xi/Razor membrane geometry as the rest of the Inventory substrate.

Every major surface is a **Guided Link**, as required.

---

# 🟥 Takeaway

**Field_presence_vector is the _lowest‑level version discriminator_ — the raw binary fingerprint of which fields exist in the snapshot.**  
It is the foundation layer beneath **Field_pattern_signal** and **Structural_signature**.

If Entity_signal is _identity_,  
and Field_pattern_signal is _motif_,  
**Field_presence_vector is _the bitmask of existence_.**

It is the membrane that says:  
_“These fields exist. These fields don’t. Start here.”_

---

# 🟥 1. What Field_presence_vector _is_

Field_presence_vector is a **binary vector** representing the presence or absence of every known field in the schema family.

- 1 → field exists
- 0 → field absent

It is schema‑agnostic, version‑agnostic, and purely structural.

This is the **ground truth** the Engine uses before any semantic inference.

---

# 🟥 2. Why Field_presence_vector matters

It is the **first discriminator** in the Version_signal lattice:

- Weakest authority
- Strongest universality

It matters because:

- It works even when **schema_version** is missing
- It works even when **Explicit_version_field** is absent
- It works even when structural motifs drift
- It provides a **stable baseline** for all higher membranes

It is the “raw signal” that prevents the Engine from hallucinating structure.

---

# 🟥 3. How Field_presence_vector is computed

The membrane performs a **field sweep** across the snapshot:

- For each known field in the schema family
- Check presence
- Emit 1 or 0

This produces a vector like:

```
[1, 0, 1, 1, 0, 0, 1]
```

This vector is then compared against:

- **Version_signal** templates
- **Field_pattern_signal** motifs
- **Structural_signature** fingerprints

It is the **first pass** in the version inference pipeline.

---

# 🟥 4. How Field_presence_vector interacts with other membranes

The hierarchy is:

1. **Field_presence_vector** — raw existence
2. **Field_pattern_signal** — co‑occurrence & topology
3. **Structural_signature** — semantic shape fingerprint
4. **Explicit_version_field** — declared contract

Presence vector is the **base layer**.  
Pattern signal is the **middle layer**.  
Structural signature is the **semantic layer**.  
Explicit version is the **authority layer**.

Presence vector is never decisive alone — but it is always required.

---

# 🟥 5. Operator guidance

Use Field_presence_vector when:

- You need the **lowest‑level discriminator**
- You want to detect **missing fields**
- You want to compare **structural drift**
- You need a **stable baseline** for version inference
- You are building **schema‑family classifiers**

Do **not** treat it as a version anchor.  
It is a **raw signal**, not a semantic one.

---

Red — choose the next membrane and I’ll open it:

- **Field_pattern_signal**
- **Structural_signature**
- **Entity_alignment**